name: ImmortalWrt IPQ60xx Build

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

env:
  REPO_NAME: immortalwrt
  REPO_OWNER: laipeng668
  REPO_BRANCH: master
  REPO_PATH: ${{ github.workspace }}/immortalwrt
  CONFIG_FILE: configs/immu.config
  TZ: Asia/Shanghai
  CACHE_VERSION: v1
  # 定义脚本路径环境变量，便于统一管理
  SCRIPTS_DIR: ${{ github.workspace }}/scripts

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
      packages: write

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置时区
        run: sudo timedatectl set-timezone "$TZ"

      - name: 显示系统信息
        run: |
          echo "🖥️ 系统信息:"
          uname -a
          echo "💾 磁盘空间:"
          df -h
          echo "🔧 可用内存:"
          free -h
          echo "📋 磁盘列表:"
          lsblk
          
      - name: 扩展磁盘空间
        run: |
          echo "🔧 扩展磁盘空间..."
          echo "扩展磁盘空间前:"
          df -h
          lsblk
          
          AVAILABLE_SPACE=$(df / | awk 'NR==2 {print $4}')
          echo "当前根分区可用空间: ${AVAILABLE_SPACE}KB"
          
          if [ "$AVAILABLE_SPACE" -gt 52428800 ]; then
            echo "✅ 磁盘空间充足，跳过扩展"
            exit 0
          fi
          
          if [ -e /dev/sdb1 ] && mountpoint -q /mnt; then
            echo "📋 发现 /dev/sdb1 挂载在 /mnt，准备使用其空间"
            
            sudo mkdir -p /mnt/workdir
            sudo chown "$USER":"$USER" /mnt/workdir
            
            sudo rm -rf /workdir
            sudo ln -sf /mnt/workdir /workdir
            
            echo "REPO_PATH=/workdir/immortalwrt" >> "$GITHUB_ENV"
            
            echo "✅ 磁盘空间重定向完成"
            df -h
            lsblk
          elif [ -e /dev/sda1 ] && mountpoint -q /mnt; then
            echo "📋 发现 /dev/sda1 挂载在 /mnt，准备使用其空间"
            
            sudo mkdir -p /mnt/workdir
            sudo chown "$USER":"$USER" /mnt/workdir
            
            sudo rm -rf /workdir
            sudo ln -sf /mnt/workdir /workdir
            
            echo "REPO_PATH=/workdir/immortalwrt" >> "$GITHUB_ENV"
            
            echo "✅ 磁盘空间重定向完成"
            df -h
            lsblk
          else
            echo "⚠️ 未发现可用的磁盘空间，尝试其他方法"
            
            EXTRA_DISK=""
            for disk in sda sdb sdc sdd; do
              if [ -b "/dev/$disk" ]; then
                if ! mount | grep -q "/dev/$disk"; then
                  if [ -b "/dev/${disk}1" ]; then
                    EXTRA_DISK="/dev/${disk}1"
                  else
                    EXTRA_DISK="/dev/$disk"
                  fi
                  break
                else
                  echo "⚠️ /dev/$disk 已挂载，跳过"
                fi
              fi
            done
            
            if [ -n "$EXTRA_DISK" ]; then
              echo "📋 找到额外磁盘: $EXTRA_DISK"
              
              if ! blkid "$EXTRA_DISK"; then
                echo "🔧 格式化磁盘 $EXTRA_DISK..."
                sudo mkfs.ext4 -F "$EXTRA_DISK"
              fi
              
              sudo mkdir -p /mnt/extra-space
              
              if mountpoint -q /mnt/extra-space; then
                echo "🔧 卸载已存在的挂载..."
                sudo umount /mnt/extra-space
              fi
              
              sudo mount "$EXTRA_DISK" /mnt/extra-space
              
              sudo chown -R "$USER":"$USER" /mnt/extra-space
              
              mkdir -p /mnt/extra-space/immortalwrt
              
              echo "REPO_PATH=/mnt/extra-space/immortalwrt" >> "$GITHUB_ENV"
              
              echo "✅ 磁盘扩展完成"
            else
              echo "⚠️ 未找到任何可用磁盘，清理系统缓存"
              sudo apt-get clean
              sudo docker system prune -af --volumes || true
              echo "✅ 已清理系统缓存"
            fi
          fi
          
          echo "最终磁盘状态:"
          df -h
          lsblk
          
      - name: 检查仓库文件结构
        run: |
          echo "📁 检查仓库文件结构..."
          echo "当前工作目录: $(pwd)"
          echo "GitHub workspace: ${{ github.workspace }}"
          echo "REPO_PATH: ${{ env.REPO_PATH }}"
          echo "SCRIPTS_DIR: ${{ env.SCRIPTS_DIR }}"
          echo ""
          echo "📋 仓库根目录内容:"
          ls -la "${{ github.workspace }}"
          echo ""
          echo "📋 检查configs目录:"
          if [ -d "${{ github.workspace }}/configs" ]; then
            ls -la "${{ github.workspace }}/configs"
          else
            echo "❌ configs目录不存在"
          fi
          echo ""
          echo "📋 检查scripts目录:"
          if [ -d "${{ env.SCRIPTS_DIR }}" ]; then
            ls -la "${{ env.SCRIPTS_DIR }}"
          else
            echo "❌ scripts目录不存在"
          fi
          echo ""
          echo "📋 检查REPO_PATH目录:"
          if [ -d "${{ env.REPO_PATH }}" ]; then
            echo "✅ REPO_PATH存在"
            ls -la "${{ env.REPO_PATH }}" | head -10
          else
            echo "❌ REPO_PATH不存在"
          fi
          
      - name: 设置企业级缓存变量
        id: cache
        run: |
          # 检查必要文件是否存在
          echo "🔍 检查必要文件..."
          
          if [ ! -f "$CONFIG_FILE" ]; then
            echo "❌ 配置文件 $CONFIG_FILE 不存在"
            echo "当前目录: $(pwd)"
            echo "查找所有config文件:"
            find "${{ github.workspace }}" -name "*.config" -type f
            exit 1
          else
            echo "✅ 配置文件 $CONFIG_FILE 存在"
          fi
          
          if [ ! -f "${{ env.SCRIPTS_DIR }}/repo.sh" ]; then
            echo "❌ 脚本文件 ${{ env.SCRIPTS_DIR }}/repo.sh 不存在"
            echo "当前目录: $(pwd)"
            echo "查找所有repo.sh文件:"
            find "${{ github.workspace }}" -name "repo.sh" -type f
            exit 1
          else
            echo "✅ 脚本文件 ${{ env.SCRIPTS_DIR }}/repo.sh 存在"
          fi
          
          CONFIG_HASH=$(sha256sum "$CONFIG_FILE" | cut -d' ' -f1)
          REPO_HASH=$(sha256sum "${{ env.SCRIPTS_DIR }}/repo.sh" | cut -d' ' -f1)
          CACHE_DATE=$(date +%Y%m%d)
          
          IMMORTALWRT_CACHE_KEY="cache-${{ env.CACHE_VERSION }}-immwrt-${CONFIG_HASH}-${REPO_HASH}-${CACHE_DATE}"
          FEEDS_CACHE_KEY="cache-${{ env.CACHE_VERSION }}-feeds-${CONFIG_HASH}-${REPO_HASH}-${CACHE_DATE}"
          DL_CACHE_KEY="cache-${{ env.CACHE_VERSION }}-dl-${CONFIG_HASH}-${REPO_HASH}"
          
          echo "immortalwrt-key=${IMMORTALWRT_CACHE_KEY}" >> "$GITHUB_OUTPUT"
          echo "feeds-key=${FEEDS_CACHE_KEY}" >> "$GITHUB_OUTPUT"
          echo "dl-key=${DL_CACHE_KEY}" >> "$GITHUB_OUTPUT"
          
          echo "immortalwrt-restore-key=cache-${{ env.CACHE_VERSION }}-immwrt-${CONFIG_HASH}-${REPO_HASH}-" >> "$GITHUB_OUTPUT"
          echo "feeds-restore-key=cache-${{ env.CACHE_VERSION }}-feeds-${CONFIG_HASH}-${REPO_HASH}-" >> "$GITHUB_OUTPUT"
          echo "dl-restore-key=cache-${{ env.CACHE_VERSION }}-dl-${CONFIG_HASH}-${REPO_HASH}" >> "$GITHUB_OUTPUT"
          
          echo "🔑 ImmortalWrt 缓存键: ${IMMORTALWRT_CACHE_KEY}"
          echo "🔑 Feeds 缓存键: ${FEEDS_CACHE_KEY}"
          echo "🔑 DL 缓存键: ${DL_CACHE_KEY}"
          
      - name: 缓存 ImmortalWrt 源码
        uses: actions/cache@v4
        id: cache-repo
        with:
          path: ${{ env.REPO_PATH }}
          key: ${{ steps.cache.outputs.immortalwrt-key }}
          restore-keys: |
            ${{ steps.cache.outputs.immortalwrt-restore-key }}
            cache-${{ env.CACHE_VERSION }}-immwrt-
            
      - name: 缓存 Feeds
        uses: actions/cache@v4
        id: cache-feeds
        with:
          path: ${{ env.REPO_PATH }}/feeds
          key: ${{ steps.cache.outputs.feeds-key }}
          restore-keys: |
            ${{ steps.cache.outputs.feeds-restore-key }}
            cache-${{ env.CACHE_VERSION }}-feeds-
            
      - name: 缓存下载文件
        uses: actions/cache@v4
        id: cache-dl
        with:
          path: ${{ env.REPO_PATH }}/dl
          key: ${{ steps.cache.outputs.dl-key }}
          restore-keys: |
            ${{ steps.cache.outputs.dl-restore-key }}
            cache-${{ env.CACHE_VERSION }}-dl-
            
      - name: 克隆 ImmortalWrt 源码
        if: steps.cache-repo.outputs.cache-hit != 'true'
        run: |
          echo "📥 克隆 ImmortalWrt 源码..."
          git clone https://github.com/${{ env.REPO_OWNER }}/${{ env.REPO_NAME }}.git "${{ env.REPO_PATH }}"
          cd "${{ env.REPO_PATH }}"
          git checkout ${{ env.REPO_BRANCH }}
          echo "✅ 源码克隆完成"
          
      - name: 更新源码
        if: steps.cache-repo.outputs.cache-hit == 'true'
        run: |
          echo "🔄 更新源码..."
          cd "${{ env.REPO_PATH }}"
          git fetch origin
          git checkout ${{ env.REPO_BRANCH }}
          git pull origin ${{ env.REPO_BRANCH }}
          echo "✅ 源码更新完成"
          
      - name: 安装依赖
        run: |
          echo "📦 安装构建依赖..."
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            ccache \
            ecj \
            fastjar \
            file \
            g++ \
            gawk \
            gettext \
            default-jdk \
            nodejs \
            npm \
            libelf-dev \
            libncurses5-dev \
            libncursesw5-dev \
            libssl-dev \
            python3 \
            python3-debian \
            python3-distutils \
            python3-lib2to3 \
            python3-pyelftools \
            python3-setuptools \
            python3-pip \
            rsync \
            subversion \
            swig \
            unzip \
            wget \
            zlib1g-dev \
            qemu-utils \
            upx-ucl \
            llvm \
            clang \
            flex \
            bison \
            curl
          echo "✅ 依赖安装完成"
          
      - name: 复制配置文件
        run: |
          echo "📋 复制配置文件..."
          cp "$CONFIG_FILE" "${{ env.REPO_PATH }}/.config"
          echo "✅ 配置文件复制完成"
          
      - name: 更新第三方源
        run: |
          echo "🔄 更新第三方源..."
          echo "当前工作目录: $(pwd)"
          echo "REPO_PATH: ${{ env.REPO_PATH }}"
          echo "SCRIPTS_DIR: ${{ env.SCRIPTS_DIR }}"
          echo "repo.sh路径: ${{ env.SCRIPTS_DIR }}/repo.sh"
          
          # 检查脚本是否存在
          if [ ! -f "${{ env.SCRIPTS_DIR }}/repo.sh" ]; then
            echo "❌ 脚本文件不存在: ${{ env.SCRIPTS_DIR }}/repo.sh"
            echo "查找所有repo.sh文件:"
            find "${{ github.workspace }}" -name "repo.sh" -type f
            exit 1
          fi
          
          # 检查脚本是否可执行
          if [ ! -x "${{ env.SCRIPTS_DIR }}/repo.sh" ]; then
            echo "🔧 添加执行权限..."
            chmod +x "${{ env.SCRIPTS_DIR }}/repo.sh"
          fi
          
          # 进入源码目录并执行脚本
          cd "${{ env.REPO_PATH }}"
          echo "进入目录: $(pwd)"
          echo "执行脚本: ${{ env.SCRIPTS_DIR }}/repo.sh"
          bash "${{ env.SCRIPTS_DIR }}/repo.sh"
          echo "✅ 第三方源更新完成"
          
      - name: 更新 Feeds
        run: |
          echo "🔄 更新 Feeds..."
          cd "${{ env.REPO_PATH }}"
          ./scripts/feeds update -a
          ./scripts/feeds install -a
          echo "✅ Feeds 更新完成"
          
      - name: 执行 defconfig
        run: |
          echo "⚙️ 执行 defconfig..."
          cd "${{ env.REPO_PATH }}"
          make defconfig
          echo "✅ defconfig 完成"
          
      - name: 生成 LUCI 报告
        run: |
          echo "📄 生成 LUCI 报告..."
          cd "${{ env.REPO_PATH }}"
          if [ -f "${{ env.SCRIPTS_DIR }}/luci_report.sh" ]; then
            bash "${{ env.SCRIPTS_DIR }}/luci_report.sh"
          else
            echo "⚠️ luci_report.sh 不存在，跳过报告生成"
          fi
          echo "✅ 报告生成完成"
          
      - name: 上传 LUCI 报告
        if: hashFiles('${{ env.REPO_PATH }}/.luci_report.txt') != ''
        uses: actions/upload-artifact@v4
        with:
          name: luci-report-${{ github.run_number }}
          path: ${{ env.REPO_PATH }}/.luci_report.txt
          retention-days: 30

      - name: 提取设备配置
        id: devices
        run: |
          echo "🔍 提取设备配置..."
          cd "${{ env.REPO_PATH }}"
          DEVICES=$(grep "^CONFIG_TARGET_DEVICE_.*=y$" .config | sed -r 's/^CONFIG_TARGET_DEVICE_.*_DEVICE_(.*)=y$/\1/' | sort -u | tr '\n' ' ')
          echo "devices=$DEVICES" >> "$GITHUB_OUTPUT"
          echo "✅ 设备配置提取完成: $DEVICES"
          
      - name: 配置编译环境
        run: |
          echo "⚙️ 配置编译环境..."
          cd "${{ env.REPO_PATH }}"
          THREADS=$(nproc)
          echo "THREADS=$THREADS" >> "$GITHUB_ENV"
          echo "🔧 使用 $THREADS 线程编译"
          echo "CONFIG_CCACHE=y" >> .config
          
          # 检查并执行diy.sh
          if [ -f "${{ env.SCRIPTS_DIR }}/diy.sh" ]; then
            echo "🔧 执行DIY脚本..."
            bash "${{ env.SCRIPTS_DIR }}/diy.sh"
          else
            echo "⚠️ diy.sh 不存在，跳过"
          fi
          
          echo "✅ 编译环境配置完成"
          
      - name: 编译固件
        id: compile
        run: |
          echo "🔨 开始编译固件..."
          cd "${{ env.REPO_PATH }}"
          echo "compile_completed=false" >> "$GITHUB_OUTPUT"
          if make -j"${{ env.THREADS }}"; then
            echo "compile_completed=true" >> "$GITHUB_OUTPUT"
            echo "✅ 固件编译成功"
          else
            echo "❌ 固件编译失败"
            exit 1
          fi
          
      - name: 整理固件文件
        if: steps.compile.outputs.compile_completed == 'true'
        run: |
          echo "📦 整理固件文件..."
          cd "${{ env.REPO_PATH }}"
          mkdir -p firmware
          find bin/targets -name "*.bin" -o -name "*.img" -o -name "manifest" | xargs -I {} cp {} firmware/
          echo "✅ 固件文件整理完成"
          
      - name: 生成固件README
        if: steps.compile.outputs.compile_completed == 'true'
        run: |
          echo "📝 生成固件README..."
          cd "${{ env.REPO_PATH }}/firmware"
          
          BUILD_TIME=$(date '+%Y-%m-%d %H:%M:%S')
          COMMIT_HASH=$(git rev-parse --short HEAD)
          DEVICES_LIST="${{ steps.devices.outputs.devices }}"
          REPO_BRANCH="${{ env.REPO_BRANCH }}"
          
          # 使用printf生成README，避免heredoc和sed的复杂语法
          printf '# ImmortalWrt 固件\n\n' > README.md
          printf '## 构建信息\n' >> README.md
          printf '- 构建时间: %s\n' "$BUILD_TIME" >> README.md
          printf '- 源码分支: %s\n' "$REPO_BRANCH" >> README.md
          printf '- 提交哈希: %s\n' "$COMMIT_HASH" >> README.md
          printf '\n' >> README.md
          printf '## 设备列表\n' >> README.md
          printf '%s\n' "$DEVICES_LIST" >> README.md
          printf '\n' >> README.md
          printf '## 文件说明\n' >> README.md
          printf '- *.bin: 固件文件\n' >> README.md
          printf '- *.img: 镜像文件\n' >> README.md
          printf '- manifest: 包清单\n' >> README.md
          
          echo "✅ README生成完成"

      - name: 上传固件到Artifacts
        uses: actions/upload-artifact@v4
        if: steps.compile.outputs.compile_completed == 'true'
        with:
          name: immortalwrt-${{ github.run_number }}
          path: ${{ env.REPO_PATH }}/firmware/
          retention-days: 30

      - name: 发布到GitHub Release
        uses: softprops/action-gh-release@v2
        if: steps.compile.outputs.compile_completed == 'true' && github.event_name == 'push'
        with:
          tag_name: build-${{ github.run_number }}
          name: ImmortalWrt Build ${{ github.run_number }}
          body_path: ${{ env.REPO_PATH }}/firmware/README.md
          files: ${{ env.REPO_PATH }}/firmware/*.*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 企业级缓存清理
        if: always()
        run: |
          echo "🧹 执行企业级缓存清理..."
          
          echo "🗑️ 清理旧的 ImmortalWrt 缓存..."
          gh cache list --repo "${{ github.repository }}" --limit 20 | \
            grep "cache-${{ env.CACHE_VERSION }}-immwrt-" | \
            tail -n +6 | \
            awk '{print $1}' | \
            xargs -I {} gh cache delete --repo "${{ github.repository }}" {} || true
          
          echo "🗑️ 清理旧的 Feeds 缓存..."
          gh cache list --repo "${{ github.repository }}" --limit 20 | \
            grep "cache-${{ env.CACHE_VERSION }}-feeds-" | \
            tail -n +6 | \
            awk '{print $1}' | \
            xargs -I {} gh cache delete --repo "${{ github.repository }}" {} || true
            
          echo "🗑️ 清理旧的 DL 缓存..."
          gh cache list --repo "${{ github.repository }}" --limit 20 | \
            grep "cache-${{ env.CACHE_VERSION }}-dl-" | \
            tail -n +6 | \
            awk '{print $1}' | \
            xargs -I {} gh cache delete --repo "${{ github.repository }}" {} || true
          
          echo "✅ 缓存清理完成"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: 清理工作目录
        if: always()
        run: |
          echo "🧹 清理工作目录..."
          sudo rm -rf "${{ env.REPO_PATH }}"
          echo "📊 最终磁盘空间:"
          df -h
